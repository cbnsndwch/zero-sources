# Microsoft Clarity Analytics
# Get your project ID from https://clarity.microsoft.com/
# Create a new project and copy the project ID from the setup instructions
VITE_CLARITY_PROJECT_ID=your_clarity_project_id_here

# GitHub Integration for Feedback (Server-side only)
# Create a personal access token at https://github.com/settings/tokens
# Required scopes: repo (for creating issues)
# IMPORTANT: These do NOT have the VITE_ prefix to keep them server-side only
GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_REPO_OWNER=cbnsndwch
GITHUB_REPO_NAME=zero-sources

# NocoDB Integration for Feedback Storage (Server-side only)
# Your NocoDB instance URL and API token
# Required tables: feedback_users, feedback_submissions
# IMPORTANT: These do NOT have the VITE_ prefix to keep them server-side only
DB_URL=https://your-nocodb-instance.com
DB_TOKEN=your_nocodb_api_token_here
