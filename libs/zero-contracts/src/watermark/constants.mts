/**
 * A dependency injection token for the watermark service. Use this token to
 * provide/inject your custom watermark service implementation.
 */
export const TOKEN_WATERMARK_SERVICE = Symbol.for('TOKEN_WATERMARK_SERVICE');
